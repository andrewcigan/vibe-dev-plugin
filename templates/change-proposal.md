# Детализация: <feat-id> — <название фичи>

> Шаблон стадии детализации (v8 L2-F2/F3, OpenSpec change + spec-kit). Кладётся в
> `docs/changes/<feat-id>/proposal.md`. Обязателен для M/L-фич ПЕРЕД входом в active
> (гейт state-transition.sh блокирует M/L в active без этого файла с хотя бы одной P1-US).
> Рядом — `tasks.md` (чек-лист задач; незакрытые `[ ]` не дают заархивировать фичу, L3-F6).

## Proposal (что и зачем)

- **Что делаем:** [одно-два предложения — суть изменения]
- **Зачем (бизнес-эффект):** [какую боль/ценность закрывает — на языке пользователя]
- **Провенанс:** [откуда требование — встреча/диалог/…; ссылка на источник; синхронно с provenance-головой в feature_list.json]

## User Stories (приоритизированные, независимо тестируемые)

> Каждая US — отдельный демо-срез (independently testable). Приоритеты P1 (обязательно для MVP)
> → Pn. **Минимум одна P1** с Acceptance в Given/When/Then — это основа `verification_command`
> и критерия «готово = проверенное поведение».

### P1 — [название истории]
Как [роль], я хочу [действие], чтобы [ценность].

**Acceptance (Given/When/Then):**
- **Given** [исходное состояние/данные]
- **When** [действие пользователя/системы]
- **Then** [наблюдаемый результат — то, что проверяет verification]

### P2 — [название] *(опционально)*
Как [роль], я хочу [действие], чтобы [ценность].
- **Given** … **When** … **Then** …

## Design (коротко — как, без кода)

- **Подход:** [ключевое архитектурное решение + одно-два последствия/trade-off]
- **Затрагиваемые части:** [компоненты/сущности; синхронно с affected_files]
- **Риски/edge-cases:** [что может пойти не так; граничные случаи]

## Tasks

См. `tasks.md` в этой же папке (чек-лист `- [ ]`; закрывается по мере работы; незакрытые
блокируют архивацию фичи при /ship — L3-F6).
