# Vibe Dev v8

> 🌐 **English:** [README.md](README.md) · **Русский:** этот файл

**Harness-first pipeline от бизнес-идеи до работающего продукта — плагин для Claude Code.**

Плагин берёт идею у предпринимателя-непрограммиста и автономно ведёт её до работающего
продукта: агент сам принимает технические решения, а человек остаётся на уровне бизнеса
и архитектуры. Главный принцип:

> ## «Harness — это enforcement, не documentation.»
> Каждый принцип имеет **реальный механизм** (hook / gate / agent / self-check), а не строчку
> в инструкции, которую агент может проигнорировать. Дисциплину ломает ровно то звено, которое
> должно её соблюдать — сам агент. Поэтому правила превращены в проверяемые «посты».

Число механизмов и их живой статус — в [`docs/traceability.md`](docs/traceability.md),
единственном источнике истины (сейчас **67 отслеживаемых**; часть строк честно помечена как
дисциплина или экранный слой — например язык-ловец на экране, маска секретов в выводе, нудж на
`/checkpoint` — и в счёт enforcement **не** идёт). Каждый механизм — с тремя атрибутами:
*где зафиксирован / чем enforce / что произойдёт при попытке обойти*. Полноту проверяет
self-check плагина — строка без механизма не пройдёт.

**Новое в v8:** история фич стала неперезаписываемой (провенанс как журнал событий),
разгрузка контекста — управляемой командой `/checkpoint` вместо «рулетки» авто-сжатия, а
проверяющий агент физически лишён права писать код. **В v7** появилась автопамять (слепок перед
сжатием + бриф возврата), браузерный тестировщик, который смотрит на страницу глазами, и замки на
секреты. Все новые сторожа проверены живыми прогонами на движке Claude Code 2.1.x — дата проверки
стоит в строке каждого механизма.

---

## Как это работает (харнесс простыми словами)

У Claude Code есть ключевые моменты, которые он объявляет: «сейчас сохраню файл», «выполню
команду», «показываю сообщение человеку», «открываю/закрываю сессию». Плагин **вешает на эти
моменты маленьких инспекторов** (hooks). Каждый инспектор смотрит на намерение и выносит один
из вердиктов:

- **block** — действие отменяется (например, нельзя пометить фичу «готово» без доказательства);
- **warn / inject** — действие проходит, но появляется пометка для агента или флаг для человека;
- **pass** — всё чисто, молча пропустить.

Список «на таком-то событии — позови такого-то инспектора» лежит в `hooks/hooks.json` и
**подхватывается автоматически** при установке (Claude Code v2.1+) — ручная настройка не нужна.
Часть постов стоит вне плагина — в `git pre-commit` проекта: они работают, даже если плагин
снесли. Строгость регулируется на проект: `minimal` / `standard` / `strict` (живые проекты не
ломаются — переводятся командой `/upgrade-project`, в том числе мягко: `--soft`).

---

## Механизмы — что каждый ловит (ключевые)

### A. «Готово» = проверено, а не на словах
| Механизм | Что ловит | Что делает |
|---|---|---|
| **UI-evidence gate** | UI-фичу помечают «готово» по typecheck/тестам, но пользователь нажал — пусто | **block** (нужен скриншот/живой прогон) |
| **Evidence по поверхности** (v6.2) | фича «без UI» (API/задача по расписанию/CLI) закрывается без следа реального вызова; UI-фича маскируется под «библиотеку» | поверхность определяется по файлам и только ужесточается: ui → **block**, остальные → **warn** с инструкцией приёмки |
| **Runtime-след на логике + негативный тест** (v8) | «зелёные тесты» лгут: логическая фича закрыта на одном typecheck; средняя/большая — без проверки «а если сломать» | passing без следа реального прогона → **block**; M/L без mutation- или leak-проверки → **block** |
| **Независимый проверяющий** (v8) | тот же агент и пишет код, и «подтверждает», что он работает | верификатор запускается с установкой «считай сломанным, пока не доказано» и **физически лишён права писать** (движок запрещает ему Write/Edit) |
| **Критика до реализации** (H7) | средняя/большая фича уходит в работу без продуманной стратегии проверки | **block** (нет `docs/test-strategy.md` — нельзя в active) |
| **Стадия детализации фичи** (v8) | крупную фичу тащат в работу «на словах», без разложенного плана | M/L-фича не входит в работу без `docs/changes/<id>/proposal.md` с приоритетной пользовательской историей в форме «дано/когда/тогда» → **block** |
| **Ревью модели данных** | пишут схему БД без отдельного критического ревью (модель «застывает», переделка дорогая) | **block** (нет `docs/data-model-review.md` — нельзя в active) |
| **State-machine переходов** | фича прыгает в невалидное состояние / битый служебный файл | **block** (актуальный проект) / warn (старый) |

### A2. Активация хуков — проверяемый факт (v6.2)
| Механизм | Что ловит | Что делает |
|---|---|---|
| **Heartbeat** | хуки «вроде установлены», но физически не работают (тихий театр строгости) | каждое живое событие пишет отметку с версией; читатели сверяют свежесть |
| **Двухфазный профиль** | профиль «strict» записан, а enforcement не включался | bootstrap пишет `pending-strict`; в боевой `strict` переводит ТОЛЬКО живой хук — факт перевода = доказательство |
| **Git pre-commit backstop** | плагин снесли/сломали — и никто не заметил | НЕЗАВИСИМЫЙ от плагина пост в `.git/hooks`: pending-профиль или протухший heartbeat → **block** коммита |
| **Fail-loud + crash-артефакты** | сторож упал и молча «разрешил всё» (реальный баг 2026-06-06) | краш → громкое предупреждение + лог краша + probe при старте сессии |
| **Корпус реальных форм** | гейт зелёный на синтетике, падает на боевых файлах | self-check гоняет гейт на 6 обезличенных боевых `feature_list` |
| **`/doctor`** | «почему сторожа молчат?» | самодиагностика: профиль/heartbeat/краши/установка + таблица починки |

### A3. История фич не переписывается (провенанс, v8)
| Механизм | Что ловит | Что делает |
|---|---|---|
| **Журнал событий только на дозапись** | заднее число: прошлую строку истории правят или стирают | правка/удаление строки журнала → **reject** коммита |
| **Инвариант правки требования** | требование фичи (название, описание, размер, бизнес-инвариант) тихо переписали или фичу «отменили» без следа | правка без покрывающего события истории → **reject** коммита; обычный прогресс работы (в работе → проверено → готово) свободен |
| **Crash-safe запись** | обрыв посреди записи рвёт состояние | единственный путь записи пишет журнал и «голову» в безопасном порядке: обрыв восстановим повтором, не потерей |
| **Архив по ссылке + отпечаток доказательства** | завершённые фичи раздувают горячий файл, а доказательство «готово» можно подменить | тело уходит в архив, в рабочем файле остаётся однострочная ссылка; отпечаток (hash) сверяется на коммите **без загрузки тела в контекст** |
| **Гейт незакрытых задач при выпуске** | фичу архивируют с незавершёнными пунктами внутри | архивация → **block**, пока задачи не закрыты |

### A4. Контекст под контролем, а не «как повезёт» (v8)
| Механизм | Что ловит | Что делает |
|---|---|---|
| **Управляемый `/checkpoint`** | состояние живёт только в переписке — авто-сжатие съедает его в случайный момент | чекпоинт по команде: восстановление истории → архивация завершённого → **cold-start gate**: шаблонный или необновлённый `SESSION.md` / рассогласованная история → **block** завершения |
| **Трёхуровневый контекст** | тело завершённой фичи годами лежит в «горячем» файле | горячий ≤200 строк (голова + указатели) / по требованию поиском / холод = архив; раздутый горячий файл → **warn** на коммите |
| **Сужение возврата читающих агентов** | субагент-исследователь вываливает сырьё в главный поток и съедает контекст | полный результат в файл, в поток — выжимка ≤2 КБ + путь (мнение критиков не режем) |
| **Слепок перед сжатием** (v7) | авто-сжатие всё же случилось — факты потерялись | перед сжатием сохраняется выжимка ФАКТОВ (не статуса «готово»); при возврате в сессию — бриф с фразой «сверься с файлами, не доверяй памяти» |

### B. Безопасность и деньги
| Механизм | Что ловит | Что делает |
|---|---|---|
| **bulk-API gate** | массовый внешний API без проверки лимитов (реальный кейс: бан проекта на 2 суток + впустую деньги) | **block** без pre-launch-checklist (теперь чеклист требует явные объём × цену) |
| **Смена модели без smoke** | правка вносит модель/настройку, влияющую на каждый ответ (реальный кейс: 3 дня обрывов клиентам после «новее = drop-in») | **warn** «это изменение контракта, прогони smoke» |
| **Vendor-lock без research** | в архитектуру вшивают конкретного поставщика наугад, без сравнения вариантов | **block** integration-фичи без `docs/research/*.md` |
| **Хардкод живого ключа** (v7) | боевой ключ вписывают прямо в исходник — и он уезжает в git | запись файла с живым ключом → **block**; снимается только явной фразой пользователя |
| **Секрет в чате** (v6.2) | пользователь вставил живой ключ в сообщение | **warn**: ключ скомпрометирован → ротация + перенос в `.env` |
| **Секрет в выводе команды** (v6.2) | CLI напечатал токен — он оседает в контексте сессии | **warn** модели: не переиспользовать литералом, предложить ротацию (+маска вывода на движках с поддержкой) |
| **Запись вне корня проекта** (v7→v8) | агент пишет файл мимо проекта (в чужую папку) | **warn** + запись в лог; корпус копится под будущую блокировку |
| **concurrent-write** | две сессии пишут в один файл (реальный кейс: потеря данных) | **warn** (advisory) |

### C. Анти-залипание
| Механизм | Что ловит | Что делает |
|---|---|---|
| **Стоп-сигнал пользователя** | человек пишет «не туда / остановись / мы не то делаем», а агент продолжает тактически | **inject** «смена уровня, не способа; запусти субагент-диагностику» |
| **Interrupt-recovery** (v6.2.1) | обрыв связи (закрытая крышка ноутбука) или доставка сообщения убили выполнявшийся инструмент — система лживо пишет «пользователь отклонил», агент стоит часами | следующее сообщение без «стоп» → **inject** «это был обрыв, не запрет — продолжай план»; настоящий «стоп» сохраняет силу |
| **Повтор падающих команд** | одна и та же команда запускается 3-й раз подряд без успеха и без структурных правок | **warn** ДО выполнения: подсказка про субагент-диагностику |
| **Circuit breaker** (v7) | и на удвоенном пороге повторов агент продолжает крутить то же самое | твёрдая эскалация в `/stuck` — залипание перестало быть вопросом дисциплины |
| **Эскалация по уровню модели** (v8) | дешёвая модель дважды не справилась, а её просят в третий раз | порядок: 2 провала → поднять уровень модели, не ретраить; общий совет нескольких LLM — только если и старшая модель провалила |

### D. Понятный язык (главная боль непрограммиста)
| Механизм | Что ловит | Что делает |
|---|---|---|
| **Clarity-gate на финал хода** (v6.2) | ход завершается оценкой в человеко-днях или тяжёлым жаргоном вне кода | **block**: агент обязан дописать то же самое простыми словами (≤10 строк); точность держит размеченный корпус из боевых сессий + лимиты дописок |
| **Язык-ловец (экранный слой)** | жаргон / развилка без «что теряешь» / человеко-дни в любом сообщении | **флаг на экране + лог-метрика** (честно display-only; в Desktop-среде событие не приходит — несущий слой выше) |
| **Онбординг (`/setup`)** | система не знает, как говорить с новым пользователем | портрет `~/.vibe-dev/portrait.md` → строгость гейтов и формат развилок подстраиваются (без портрета — нейтральный дефолт) |

### E. Дисциплина процесса
| Механизм | Что ловит | Что делает |
|---|---|---|
| **WIP=1 / scope** | правки выходят за рамки заявленной фичи | **block** коммита (diff ⊆ affected_files) |
| **Намерение без действия** | агент завершает ход словами «сейчас сделаю X» без единого действия | **block** (продолжить ход) |
| **Единый Stop-диспетчер** (v6.2) | несколько сторожей на завершении хода каскадят блокировки и зацикливают ход | приоритеты + общий предел ≤3 блокировок на ход; переполнение → pass с записью в лог |
| **Research-гейт архитектуры** (v6.2) | архитектура пишется без изучения лучших практик и готовых решений | **block** записи `ARCHITECTURE*.md` без `docs/research/*`; пропуск — ТОЛЬКО явной фразой пользователя |
| **Closing-mode** (v6.2) | «закрываем сессию» → агент вдруг начинает кодить | права деградируют: запись только в файлы состояния; новая работа → backlog; снимается обычным сообщением |
| **Lock-паттерн** (v6.2) | агент сам «изображает согласие пользователя» (маркеры пропуска/закрытия) | маркеры `.harness/locks/*` пишут ТОЛЬКО хуки по явной фразе — запись агентом **block** |
| **Config-protect** (v6.2, усилен v8) | агент ослабляет собственные гейты (профиль, heartbeat, «учебный» режим, отключение) | **block** во всех профилях, включая обходы через копирование/переименование файлов; ослабить строгость — только руками пользователя |
| **«Не тормози»** (v7) | сказано «делай до конца», а ход снова закончился техническим переспросом | **inject** «технический вопрос не задавай, продолжай; бизнес-развилку оставь» |
| **Handoff-петля** | при закрытии сессии план остаётся в чате (новая сессия его не увидит) | **inject** cold-start чеклиста + детекция пропуска при старте |
| **Правило пользователя** (`/hookify`) | «больше не делай X» забывается и повторяется | человек замораживает коррекцию в постоянное **block/warn**-правило без кода |

### F. Инфраструктура харнесса
| Механизм | Что делает |
|---|---|
| **Hooks из коробки** | авто-загрузка `hooks.json` при установке; без файла — невозможно «забыть включить» |
| **warn доходит до модели** | предупреждения идут правильным каналом (иначе молча терялись бы) |
| **Профили + version-lifecycle** | minimal/standard/strict; старые проекты не форсятся, переводятся по команде (`/upgrade-project`, мягкий режим `--soft`, предпросмотр `--dry-run`) |
| **Единый резолвер путей** (v8) | один источник имён служебных файлов + поиск корня проекта вверх по дереву; неоднозначный корень → отказ вместо тихой записи «не туда» |
| **Пины моделей по стадиям** (v8) | реестр ролей — источник истины: старшая модель на план/критику/проверку, рабочая на код/чтение; расхождение с реестром роняет self-check |
| **Единая цифра готовности в `/audit`** (v8) | здоровье проекта = МИНИМУМ по узкому месту (а не среднее), с детерминированными метриками целостности истории |
| **Таблица трассировки + self-check** | каждый механизм описан 3 атрибутами; строка без живого механизма роняет self-check |
| **Gate обезличенности** | в публичной версии случайно осталось личное (почта / клиентский проект / личный путь) — **block** self-check |

> **Честно — что осталось дисциплиной, а не механизмом:** проверка связок между модулями на
> реальном пути, «агент делает сам, не шлёт в терминал», реалистичность тестовых данных, нудж
> на `/checkpoint` по длине сессии и бюджет усилий на фичу (надёжного датчика размера контекста
> в Claude Code пока нет — не приписываем себе сигнал, которого нет). Их хуком надёжно не
> заставить. Держим дисциплиной + ловим на реальном проекте. Не выдаём за «железобетон».

Построено **после аудита всех ~20 реальных проектов** предыдущих версий (12 ретроспектив +
~150 заметок памяти + 6 журналов багов); v6.2 — после **аудита 54 боевых сессий**; v7 — по
**аудиту 9 боевых журналов недели**; v8 — по решениям владельца (12 карточек) + паттернам
доноров (pilotfish / OpenSpec / spec-kit) + **первому внедрению харнеса на живой продовый
проект**, откуда пришли 4 точечных фикса (v8.0.2).

---

## 7 подсистем

**Instructions** (CLAUDE.md-роутинг + domain-rules.yaml) · **State** (feature_list.json +
SESSION.md + провенанс-журнал + error-journal) · **Verification** (4 слоя + dual critique +
negative-gate + независимый проверяющий) · **Scope** (affected_files, WIP=1) ·
**Lifecycle** (init, cold-start, `/checkpoint`, clean-exit, `/upgrade-project`) ·
**Learning** (feedback-память, ретроспективы, anti-patterns) · **Cost & Safety** (bulk-gate,
concurrent-lock, secrets-scope).

---

## Команды

| Команда | Что делает |
|---|---|
| `/setup` | Онбординг: 6 простых вопросов → портрет (как с тобой общаться) |
| `/new-project` | Бизнес-интервью + bootstrap harness (4 файла на старте) |
| `/resume <project>` | Cold-start test + диф с прошлой сессией |
| `/feature <id>` | WIP=1 + dual critique (test-researcher + user-perspective-critic) |
| `/verify` | 4-слойная проверка (синтаксис + рантайм + e2e + пользователь) |
| `/checkpoint` | Управляемая фиксация состояния в файлы + разгрузка контекста (вместо «рулетки» авто-сжатия) |
| `/hookify` | «больше не делай X» → постоянное block/warn-правило |
| `/doctor` | Самодиагностика: живы ли сторожа в этом проекте |
| `/handoff` · `/end-session` | Clean-exit + фиксация состояния в файлы |
| `/audit` | Внешняя оценка харнесса + единая цифра готовности |
| `/stuck` | Стак-протокол + LLM-кворум |
| `/ship` | Финальная валидация ≥90% + ретроспектива |
| `/upgrade-project` · `/patch-projects` | Перевод живых проектов на актуальный движок (мягко или строго) |
| `/research` · `/architecture` · `/dev-plan` · … | полный список — 29 команд в `skills/` |

## Pipeline

- **FAST (5 этапов)** — внутренние инструменты, простые MVP, Telegram-боты:
  интервью → архитектура+стек → дизайн-handoff (если UI) → `/feature` loop → `/ship`.
- **FULL (10 этапов)** — продукты на рынок: идеи R1/R2 → валидация → research → архитектура+
  прототип → дизайн → wave-план → `/feature` loop → `/ship` + маркетинг-запуск.

---

## Установка

```bash
# 1. Добавить marketplace из GitHub
claude plugin marketplace add andrewcigan/vibe-dev-plugin
# 2. Установить и включить плагин
claude plugin install vibe-dev@vibe-dev
```

Или локально (для разработки самого плагина):
```bash
claude --plugin-dir "/path/to/vibe-dev-plugin"
```

> Технический id плагина — `vibe-dev` (от него зависят имена команд и установка). Версия — **8.0.2**.

---

## Версия

**v8.0.2** — четыре фикса по первому внедрению харнеса на живой продовый проект: инструкция
перевода проектов догнала код, служебные файлы харнеса больше не «грязнят» git, сторож смены
модели перестал шуметь на документации, справочный граф переходов честно помечен справочным.

**v8.0.1** — фикс дефекта ядра (обычный прогресс работы ошибочно считался правкой требования и
отвергал первый же коммит после проверки) + безопасный перевод живых проектов на v8: мягкий
режим `--soft`, честный предпросмотр `--dry-run`, оркестратор `/patch-projects`.

**v8.0.0** — Девять волн, 26 фич, **67 механизмов**. Пять линий: провенанс фич как журнал
событий (неперезаписываемая история + архив по ссылке с отпечатком доказательства); стадия
детализации крупной фичи; управляемый `/checkpoint` вместо «рулетки» авто-сжатия +
трёхуровневая модель контекста; пины моделей по стадиям и независимый проверяющий без права
писать код; проверка «готово» по реальному прогону + негативный тест. Осознанно НЕ в ядро
(честность деклараций): датчик размера контекста — надёжного сигнала в движке нет.

**v7.0.0** — пять волн по аудиту боевых журналов: доставка правок в живые проекты, честность
деклараций (мёртвые механизмы убраны из счёта), автопамять (слепок перед сжатием + бриф
возврата), новые замки (секреты, запись вне корня), поведенческий слой («не тормози»).
Браузерный тестировщик получил обязанность снять скриншот и описать увиденное глазами.

_v6.2 — enforcement как проверяемый факт (доказуемая активация хуков, fail-loud, clarity-gate
финального сообщения). v6.1 — публичный релиз: enforcement из текста в механизм + онбординг.
Полный список изменений — [`CHANGELOG.md`](CHANGELOG.md)._
