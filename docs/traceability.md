# Таблица трассировки механизмов — Vibe Dev v6

> **Живой документ + механизм.** Каждый заявленный инвариант обязан иметь все 3 атрибута
> (тест 3 атрибутов из `~/CLAUDE.md`). Полноту проверяет `scripts/check-traceability.sh`:
> строка без 3 заполненных колонок ИЛИ со ссылкой на несуществующий файл → self-check **падает**.
> Это не документация «на доверии» — это проверяемый контракт. Запускается в self-check плагина.
>
> **Правила формата (иначе self-check падает):**
> - 4 непустых колонки на строку: Механизм | Где зафиксирован | Чем enforce | Что при обходе.
> - «Где зафиксирован» содержит ≥1 реально существующий путь-файл (относительно корня плагина).
> - «Что при обходе» содержит одно из слов: block / warn / log / ask / safe.
>
> Добавляешь новый механизм enforcement в плагин → добавляешь строку сюда. Нет строки = механизм
> не отслеживается. Строка с пустым атрибутом или мёртвой ссылкой = это пожелание, не механизм.

| Механизм | Где зафиксирован | Чем enforce | Что при обходе |
|---|---|---|---|
| Хуки активны из коробки | hooks/hooks.json | Claude Code авто-загрузка при установке (v2.1+) | без файла невозможно; cold-start кричит «ENFORCEMENT OFF» — log |
| UI-evidence gate | hooks/checks/state-transition.sh | dispatch валидирует вносимое содержимое feature_list.json (намерение, не диск) | UI→passing без layer_4/5 user-evidence = **block** (hard, всегда, даже legacy/learn) |
| State-machine структура | hooks/checks/state-transition.sh | валидатор переходов против schemas/feature-state-transitions.yaml | невалидный state / битый JSON = block (актуальный) или warn (legacy/learn) |
| bulk-API gate | hooks/checks/bulk-api.sh | dispatch на Bash, детект массового API в команде → требует pre-launch-checklist | массовый внешний API без approved checklist = **block** (во всех профилях) |
| concurrent-write | hooks/checks/concurrent-write.sh | session-marker в .harness/locks, TTL 120с | другая сессия писала shared-файл < TTL = **warn** (advisory; mutual exclusion негарантируем) |
| warn доходит до модели (R2) | hooks/lib/hook-io.sh | stdout JSON additionalContext + exit 0 (не stderr) | иначе предупреждение молча терялось бы → теперь warn виден модели |
| version lifecycle (H2) | scripts/upgrade-project.sh | .harness/engine-version + /upgrade-project; новые проекты strict, старые legacy | старый проект не форсится на strict автоматически = safe (перевод по команде) |
| WIP=1 scope | hooks/pre-commit-scope.sh | git pre-commit hook (exit 1 блокирует commit) | diff вне feature.affected_files = block коммита |
| Критика до реализации (H7) | hooks/checks/state-transition.sh | active-gate: M/L-фича в active требует docs/test-strategy.md с её id | критику пропустили = фича не входит в active = **block** |
| Ревью модели данных | hooks/checks/state-transition.sh | active-gate: data-фича требует docs/data-model-review.md с её id; критик — agents/data-model-reviewer.md. v7 P7: триггер сужен до путей ОПРЕДЕЛЕНИЯ схемы (migrations/, prisma/schema, drizzle/schema, *.sql, schema.*, schema/) — не любое вхождение слова «schema»; escape-маркер data_model_predefined снимает гейт (модель по внешнему стандарту). Проверено крафт-корпусом 2026-07-02 | схема без ревью = фича не входит в active = **block** (уровень НЕ понижен по размеру — правила #1/#11) |
| Surface: evidence по поверхности + монотонная строгость (v6.2 F5) | hooks/checks/state-transition.sh + rules/verification-lanes.md + templates/feature_list.json (поле surface) + skills/feature/SKILL.md (path по поверхности) | effective_surface = max_strict(declared surface/category, файловая эвристика) — заявленное поле только УЖЕСТОЧАЕТ (анти-регрессия UI-gate); ui → layer_4/5 hard block (механизм 2); api/job/service/cli → passing без evidence реального вызова = warn (мягкий ввод v6.2, ужесточение после обкатки); mismatch declared<эвристика → warn | UI-фича не пройдёт в passing под видом lib = **block**; пустое evidence остальных поверхностей = **warn** с lane-инструкцией |
| Намерение без действия (H19) | hooks/checks/stop-intent-without-action.sh | dispatch-stop.sh на Stop: маркер-намерение в тексте хода + ноль tool_use в ходе (из transcript) | завершение хода обещанием действия без выполнения = **block** (продолжить; cap 8, standard/strict) |
| Единый Stop-dispatcher: приоритеты + cap цепочки (v6.2 F3) | hooks/dispatch-stop.sh + tests/hooks/test-stop-dispatcher.sh | один block за раз по приоритету (intent → clarity → wave-слот); общий cap ≤3 block на цепочку хода (.harness/stop-chain-count, сбрасывается UserPromptSubmit на новом промпте) | каскад/зацикливание Stop-блоков невозможны: при переполнении cap — pass + запись .harness/stop-cap-log (**log**, видно /audit) |
| Handoff loop (H6) | hooks/checks/handoff-reminder.sh + hooks/checks/handoff-pending-probe.sh | UserPromptSubmit: сигнал завершения → inject cold-start чеклиста + маркер handoff-pending; SessionStart: маркер есть и SESSION.md не обновлён после → warn о пропуске | план рискует остаться в чате → напоминание при закрытии + детекция пропуска при старте (**warn**/inject, не block; standard/strict) |
| Стоп-сигнал пользователя (анти-залипание №1) | hooks/checks/stuck-signal-reminder.sh + hooks/dispatch-user-prompt.sh | UserPromptSubmit: стоп-слова курс-коррекции в промпте → inject «смена УРОВНЯ, не способа; субагент-диагностика, не ещё одна тактическая попытка» | проигнорированный стоп-сигнал → напоминание видно модели (**warn**/inject, не block; standard/strict) |
| Повтор Bash / retry-loop (анти-залипание №2) | hooks/checks/bash-repeat-counter.sh + hooks/dispatch-pre-tool-use.sh (инкремент) + hooks/dispatch-post-tool-use.sh (сброс) | PreToolUse инкрементирует счётчик класса команды (живая модель событий 2.1.170, проверка 2026-06-10: PostToolUse на упавших с выводом Bash НЕ приходит, тихое падение даёт returnCodeInterpretation — носитель «считать падения на PostToolUse» был мёртв); 3-й подряд запуск без успеха/правок между → warn про субагент-диагностику; на удвоенном пороге (6-й, v7 Волна 4) — circuit breaker: твёрдая эскалация в /stuck (stuck из дисциплины в механизм, паттерн loop-engineering); PostToolUse-чистый-успех (нет interrupted/returnCodeInterpretation) и Edit/Write сбрасывают. Проверено 2026-07-02 (3→warn, 6→circuit, между — тихо) | слепой retry-loop одной падающей команды → подсказка видна модели (**warn**/inject, не block; standard/strict) |
| Правило пользователя (hookify R6/H9) | hooks/checks/user-rules.sh + skills/hookify/SKILL.md | PreToolUse: `.harness/user-rules.json` (tool+regex+action) применяется к командам/путям; скилл пишет правило из «не делай X» | действие, нарушающее правило пользователя → **block** или **warn** по выбору пользователя (standard/strict); только действия, не контент сообщений |
| Смена модели без smoke (дыра аудита) | hooks/checks/model-swap-guard.sh + hooks/dispatch-pre-tool-use.sh | PreToolUse: правка вносит идентификатор модели (gpt-/claude-/gemini-/…) или ключ настройки (max_tokens/temperature/reasoning/…) → warn про изменение контракта | смена зависимости, влияющей на каждый вывод, без smoke → напоминание прогнать smoke (**warn**, не block; standard/strict) |
| Vendor-lock без research (дыра аудита) | hooks/checks/state-transition.sh + skills/research/SKILL.md | active-gate: integration-фича (category=integration или providers/scraper/fetcher в affected) не входит в active без docs/research/*.md с её id | поставщик пригвождается без research → фича не входит в active (**block**; standard/strict, warn в legacy) |
| Язык-ловец: экранный слой | hooks/checks/clarity-detector.sh + hooks/dispatch-message-display.sh + hooks/lib/clarity-lexicon.sh + rules/decision-format.md | MessageDisplay: жаргон / развилка-без-«что теряешь» / человеко-дни → лог `.harness/clarity-violations.log` + флаг на экране; строгость по `jargon_tolerance` портрета | непонятное сообщение → подсвечено + посчитано (**warn/log**; display-слой; enforcement делает clarity-stop-gate строкой ниже; standard/strict). ⚠️ Среда-зависим (живая проверка 2026-06-10): в Desktop/SDK-сессиях 2.1.170 событие MessageDisplay НЕ эмитится — ловец молчит; несущий механизм ясности — clarity-gate на Stop (он живой везде) |
| Clarity-gate: enforcement финального сообщения (v6.2 F4) | hooks/checks/clarity-stop-gate.sh + hooks/lib/clarity-lexicon.sh + tests/hooks/fixtures/clarity-corpus/ + tests/hooks/test-clarity-gate.sh | Stop-хук читает `last_assistant_message` (движок ≥2.1.47): BLOCK-tier (человеко-дни-оценка, HARD-жаргон вне код-блоков) → агент дописывает аддендум ≤10 строк (не rewrite); развилка без потерь/рекомендации → warn; tier включается портретом непрограммиста или strict; ≤2 дописки на цепочку + общий cap F3; precision-гейт: false positive на good-корпусе = self-check красный | ход НЕ может закончиться только непонятным сообщением = **block** (честно: пост-фактум — первое сообщение пользователь уже видел; решабельная версия гарантированно следует) |
| Активация enforcement: heartbeat (v6.2 F2) | hooks/lib/hook-io.sh (hook_write_heartbeat) + hooks/dispatch-session-start.sh + hooks/dispatch-user-prompt.sh | SessionStart и UserPromptSubmit пишут .harness/hooks-heartbeat (unix-ts + версия плагина) на каждом срабатывании | свежий heartbeat = доказательство «хуки физически работают»; читатели: pre-commit backstop (block), /doctor, Check 0 скиллов — log |
| Активация enforcement: двухфазный профиль (v6.2 F2) | scripts/upgrade-project.sh + skills/new-project/SKILL.md + hooks/lib/hook-io.sh (hook_activate_pending_profile) | bootstrap пишет pending-strict; в боевой strict переводит ТОЛЬКО живой хук (факт перевода = доказательство активации); сами хуки читают pending-X как X (окна слабости нет) | профиль «strict» без работающих хуков невозможен по построению: остаётся pending → pre-commit backstop **block** + /doctor диагностика |
| Активация enforcement: pre-commit backstop (v6.2 F2) | templates/git-pre-commit.sh + scripts/install-precommit.sh (ставится bootstrap/upgrade) | НЕЗАВИСИМЫЙ канал в .git/hooks/ проекта (работает даже без плагина): pending-профиль ИЛИ heartbeat старше 30 мин при standard/strict → блок коммита с диагностикой; escape — .harness/hooks-disabled (осознанно) | коммит при «театре строгости» = **block** (exit 1); заодно закрыта дыра «pre-commit-scope.sh никогда не устанавливался в проекты» |
| Самодиагностика /doctor (v6.2 F2) | skills/doctor/SKILL.md | скилл: показания (профиль/heartbeat/краши/plugin list/pre-commit) + таблица диагнозов + починка | discipline-assisted (скилл-инструкция); несущие каналы — heartbeat+backstop выше; обнаруженное чинится по таблице — log |
| Секрет во входящем → ротация (v6.2 F8) | hooks/checks/secret-in-prompt.sh + hooks/dispatch-user-prompt.sh | UserPromptSubmit: жёсткие паттерны живых ключей (sk-ant/sk-or/sk-proj/ghp_/AKIA/xox/PEM; голый sk- не ловится — precision) → inject: компрометация + ротация + .env + $VAR | ключ в чате больше не сохраняется молча = **warn/inject** (block поздно — ключ уже в контексте) |
| Секрет в выводе → предупреждение + маска (v6.2 F8) | hooks/checks/secret-mask-output.sh + hooks/dispatch-post-tool-use.sh | PostToolUse(Bash, = успех): живой токен в stdout → additionalContext-предупреждение (не переиспользуй литерал, $VAR-workflow, предложи ротацию) + updatedToolOutput с маской в том же объекте; cap 64КБ. Живая проверка 2026-06-10 (2.1.170): имя поля .tool_response.stdout ВЕРНОЕ, но updatedToolOutput движком игнорируется (маска не применяется — safe-деградация) → рабочий канал = additionalContext; маска включится сама на движке с поддержкой | токен в выводе CLI не проходит тихо = **warn/inject** (ротация + $VAR); честная граница: вывод у модели остаётся немаскированным до поддержки updatedToolOutput движком |
| Cost-преview: объём×цена до вопроса (v6.2 F8) | templates/pre-launch-checklist.yaml (volume_items, price_per_item_usd) | bulk-API gate (механизм 4) уже требует заполненный чеклист — теперь чеклист требует явные объём и цену за единицу | оценка «5000 записей × $X» предъявляется ДО того, как пользователь спросит = **block** через существующий гейт 4 |
| Closing-mode: деградация прав при закрытии (v6.2 F7) | hooks/checks/closing-mode-gate.sh + hooks/dispatch-user-prompt.sh (маркер) + skills/end-session/SKILL.md | сигнал завершения → UserPromptSubmit-хук ставит `.harness/locks/closing-mode`; PreToolUse в режиме: запись только в state-файлы, Bash только git/read-only/скрипты плагина; авто-снятие следующим промптом без сигнала (инструкция пользователя главнее) | «закрой сессию» больше не может запустить разработку = **block** с маршрутом «новая работа → backlog» (standard/strict) |
| Lock-паттерн: маркеры согласия пишут только хуки (v6.2 F6) | hooks/checks/locks-protect.sh + hooks/dispatch-pre-tool-use.sh | PreToolUse во всех профилях: Write/Edit в `.harness/locks/*` и очевидный Bash-redirect туда → block; rm разрешён (движение к строгости безопасно) | агент не может сам «изобразить согласие пользователя» = **block** (честная граница: экзотический Bash-обход не ловится — threat model «пассивные ошибки») |
| Research-гейт архитектуры (v6.2 F6; распоряжение пользователя 2026-06-10) | hooks/checks/architecture-research-gate.sh + hooks/checks/research-skip-listener.sh + skills/architecture/SKILL.md (Шаг 0) | PreToolUse: запись docs/ARCHITECTURE*.md (глоб) без docs/research/*.md И без маркера `.harness/locks/research-skipped` → block; маркер ставит ТОЛЬКО UserPromptSubmit-хук по явной фразе «пропусти рисёрч» (с цитатой), скилл потребляет его одноразово | архитектура без рисёрча = **block**; пропуск возможен только явной фразой пользователя (lock-паттерн) |
| Fail-loud обвязка хуков (v6.2 F1) | hooks/lib/hook-io.sh (hook_run_check) + tests/hooks/test-failsafe.sh | все диспетчеры зовут проверки через hook_run_check: краш проверки (exit != 0) → WARN/⚠️-предупреждение в канал + crash-артефакт .harness/hook-crashes/<label>.log; SessionStart-probe сообщает о крашах прошлых сессий | краш сторожа НЕ молчит (раньше: пустой stdout = молчаливый fail-open, баг 2026-06-06); честно: краш hard-гейта временно открывает гейт с **warn** — компенсация: crash-артефакт + probe на старте + /doctor |
| Корпус реальных форм данных (v6.2 F1) | tests/hooks/fixtures/real/ (6 обезличенных боевых feature_list) + tests/hooks/test-real-fixtures.sh + fixtures/anonymize-feature-list.py | self-check раздел 19 гоняет state-transition на каждой фикстуре: реальные формы не роняют сторож + контрольная битая UI-фича ловится | гейт, зелёный только на синтетике, не пройдёт self-check = **block** (exit 1) |
| Enforcement-config-protect (v6.2 F9; R3 ECC) | hooks/checks/enforcement-config-protect.sh + tests/hooks/test-config-protect.sh | PreToolUse во всех профилях: запись .harness/profile НЕ-pending значением / запись heartbeat / создание hooks-disabled агентом (Write/Edit/Bash-redirect) → block; pending-* разрешён (bootstrap, слабейшее состояние) | агент не ослабит собственные гейты и не сфальсифицирует активацию = **block**; escape hooks-disabled — только руками пользователя |
| Стиль ≠ содержание + канон сообщений (v6.2 F9; BMAD-паттерн) | templates/portrait.md (правило + 3 канонических примера) + agents/data-model-reviewer.md (fact-forcing) | портрет несёт few-shot примеры хороших сообщений (опора clarity-gate) и железное правило «уровень влияет на стиль, не на качество работы»; reviewer требует ФАКТЫ вместо «уверен?» | discipline-assisted (шаблон/инструкция агента; несущие механизмы — clarity-gate F4 и active-gate ревью) — log |
| Interrupt-recovery: техническое прерывание ≠ запрет (v6.2.1) | hooks/checks/interrupt-recovery.sh + hooks/dispatch-user-prompt.sh + tests/hooks/test-interrupt-recovery.sh | UserPromptSubmit: хвост последнего хода содержит interrupt/reject-маркер («doesn't want to proceed/take this action», «[Request interrupted by user]»; обрыв клиента — issue anthropics/claude-code#49790 — или доставка входящего убила вызов), работа после маркера не возобновлялась, в новом промпте нет стоп-слов → inject «прерывание было техническим, продолжай план, перезапусти убитый вызов» | агент не стоит часами после лживого «user rejected» (зафиксировано 7ч17м простоя) = **warn/inject**; настоящий «стоп» пользователя главнее (стоп-словарь выключает) |
| Полнота трассировки (тест 3 атрибутов) | scripts/check-traceability.sh | парсит эту таблицу: 3 атрибута + живые ссылки | строка без 3 колонок / мёртвая ссылка = self-check **block** (exit 1) |
| Обезличенность shipped (v6.1, публичный релиз) | scripts/check-no-personal-data.sh + tests/hooks/test-no-personal-data.sh | grep по shipped-набору, вызывается из scripts/check-plugin-self.sh (раздел 17) | приватные данные (username/личная почта автора / имя реального проекта / личный путь / портрет; публичное имя автора разрешено) в shipped → self-check **block** (exit 1) |
| Канал доставки: нудж мягких (legacy) проектов на /upgrade-project (v7 Волна 0) | hooks/lib/hook-io.sh (hook_upgrade_nudge) + hooks/dispatch-session-start.sh + skills/doctor/SKILL.md | SessionStart и /doctor читают .harness/engine-version; проект в МЯГКОМ режиме (нет пина ИЛИ major<6 = legacy-порог state-machine) → inject «прогони /upgrade-project для строгости». Пин 6.x/7.x уже строгий → НЕ нудим (анти-шум: бамп версии плагина не трогает уже-строгие). Новые механизмы v7 работают НЕЗАВИСИМО от пина (грузятся из CLAUDE_PLUGIN_ROOT); пин гейтит только строгость state-machine. Проверено 2026-07-02 (нет пина/5.0 → подсказка; 6.0/7.0 → тишина) | мягкий режим не проходит тихо = **warn**/inject; само снимается после /upgrade-project |
| Слепок перед сжатием (M2, v7 Волна 2 автопамять) | hooks/pre-compact.sh + hooks/hooks.json | PreCompact-событие (подтверждено живым прогоном 2026-07-02 сквозным на 2.1.170: payload несёт transcript_path, файл существует, /compact → слепок появился сам): extractive-парсинг транскрипта (первая просьба + последние просьбы + хвост хода, фильтр шума по флагам type/isMeta/toolUseResult) → перезапись .harness/last-checkpoint.md; без LLM/субагента; пишет ФАКТЫ, НЕ статус «готово» | контекст не теряется молча при компакции = **log** (advisory, fail-open: любая ошибка → тихий пропуск; слепок — подстраховка, не гейт) |
| Бриф возврата + recall (C1, v7 Волна 2 автопамять) | hooks/lib/hook-io.sh (hook_cold_start_brief) + hooks/dispatch-session-start.sh | SessionStart: активные фичи (feature_list state=active) + наличие тупиков (error-journal) + наличие слепка M2 → компактный inject, ОБЯЗАТЕЛЬНО завершён recall-фразой «сверься с файлами, не доверяй памяти о готовности»; пусто на чистом старте; проверено живым прогоном 2026-07-02 | возврат без сверки с живым состоянием не проходит тихо = **warn**/inject (recall-фраза повышает шанс чтения, не гарантия; пишет ФАКТЫ, не статус) |
| Гигиена журнала: штамп устаревания + форма против повторов + маршрутизация + дедуп (G1/C2/C3/G5/G2, v7 Волна 2+4) | templates/error-journal.md + scripts/journal-audit.sh + skills/doctor/SKILL.md | записи журнала получают status active/superseded/expired + recorded_at + superseded_by (старое не стираем — помечаем, читаем только active); форма C2 [problem/attempted/rejected/next_step]; шапка C3 «когда сюда писать»; journal-audit.sh — READ-ONLY отчёт (записей/active/без штампа/повтор классов/дубль problem через python-нормализацию) без правок, зовётся из /doctor. Проверено 2026-07-02 | журнал не врёт и не теряет историю; аудит только докладывает = **log** (обнаружение отделено от правки, дедуп дёшево-без-субагента) |
| Хардкод живого ключа в src (P14, v7 Волна 3) | hooks/checks/secret-scan-write.sh + hooks/lib/secret-lexicon.sh + hooks/dispatch-pre-tool-use.sh | PreToolUse Write/Edit/MultiEdit во всех профилях: живой ключ (единый словарь secret-lexicon) в content/new_string исходника → block; escape — цель .env-семейство ИЛИ одноразовый маркер secret-scan-off. Проверено 2026-07-02 (живой формат → block; .env/плейсхолдер/фраза → тишина) | боевой ключ не уедет в git молча = **block**; честно превентивно (наблюдаемая утечка была в чат, вне SDK); escape одной фразой |
| Escape-фраза secret-scan (lock-паттерн, v7 Волна 3) | hooks/checks/secret-skip-listener.sh + hooks/dispatch-user-prompt.sh | UserPromptSubmit: явная фраза «ключ тестовый/забей на секрет» → хук (не агент) пишет одноразовый .harness/locks/secret-scan-off; агенту запись в locks/ запрещена (locks-protect) | «изобразить согласие» агент не может = **log**; снятие блока только явной фразой пользователя |
| wave-continue: go-режим + ход кончился вопросом (P6, v7 Волна 5) | hooks/checks/go-mode-listener.sh + hooks/checks/wave-continue.sh + hooks/dispatch-user-prompt.sh + hooks/dispatch-stop.sh | go-mode-listener: явная фраза «не тормози/делай до конца» → .harness/locks/go-mode (стоп-слово снимает; агенту в locks/ нельзя — locks-protect); wave-continue на Stop (приоритет 3, после stop-intent и clarity): go-mode И последний ход агента кончился «?» → inject «тех-переспрос не задавай, продолжай; бизнес-развилку оставь». Проверено 2026-07-02 | «завершил ход вопросом когда сказали идти» → напоминание = **warn**/inject (НЕ block: вопрос не давим — риск проскока развилки; честный предел P6) |
| Запись вне корня проекта (P9, v7 Волна 3) | hooks/checks/folder-scope.sh + hooks/dispatch-pre-tool-use.sh | PreToolUse Write/Edit: только структурный file_path (не греп тела Bash); вне корня И вне whitelist (git-worktree проекта, /tmp, ~/.vibe-dev, ~/.claude) → ТОЛЬКО-ЛОГ в .harness/folder-scope.log; WARN лишь при маркере folder-scope-warn (после обкатки корпуса). READ вне корня не трогаем. Проверено 2026-07-02 | грубый промах записи собирается в корпус (старт log-only — нетто-риск P9 под контролем) = **log** (warn опционально по маркеру) |
| Контракт фронтматтера агентов: model/effort/disallowedTools (v8 L1-F1, foundational) | docs/agent-registry.md + agents/stage-verifier.md (пример disallowedTools) + scripts/check-plugin-self.sh (раздел 4) | Движок Claude Code читает model/effort/disallowedTools из фронтматтера субагента (docs subagents-and-plugins.md — enforced, не декларация); self-check раздел 4 линтит наличие model+effort у КАЖДОГО из 24 агентов; 6 read-only ролей (data-model-reviewer/user-perspective-critic/test-researcher/stage-verifier/evaluator/browser-tester) несут disallowedTools=Write,Edit,MultiEdit,NotebookEdit — гарантия «проверяющий не пишет код» (несёт L5-F4). effort=max у всех (решение владельца c1). Реестр = единый источник роль→тир. Живой прогон 2026-07-10 (24/24 effort, 6/6 disallowed, model не изменён, self-check раздел 4 зелёный) | агент без model/effort → self-check **block** (exit 1); read-only роль физически не может Write/Edit — движок = **block** |
| Единый резолвер путей харнеса (v8 L2-F1, foundational) | hooks/lib/resolve-paths.sh + hooks/lib/hook-io.sh (source + перевод хардкодов) + tests/hooks/test-resolve-paths.sh | Единственный источник имён артефактов (feature_list/SESSION/profile/hook-mode/provenance-log/archive/docs·changes) через функции vibe_path_*; поиск корня вверх по дереву до маркера (.harness/ или feature_list.json); STRICT при неоднозначном/ненайденном корне → hard-error в stderr + ненулевой код (НЕ тихий фолбэк на cwd — лечит «запись не в тот проект»); LENIENT — тихо для хуков (guard решает). Живой прогон 2026-07-10 (self-check раздел 33: 22/22; регресс PreToolUse 43/43 + activation 22/22 + session-start 9/9 цел) | скрипт состояния (record-change/checkpoint) не пишет в неизвестное место — strict-резолв отказывает (exit≠0) = **block** записи через отказ; хуки в lenient = **safe** (пусто → guard) |

---

## Профили строгости

| Профиль | state-machine | UI-evidence | bulk-API | concurrent | Stop-intent (H19) | handoff (H6) |
|---|---|---|---|---|---|---|
| minimal | выключено | выключено | **block** | выключено | выключено | выключено |
| standard (default) | block (актуальный) / warn (legacy) | **block** | **block** | warn | **block** | inject |
| strict | block | **block** | **block** | warn | **block** | inject |

`.harness/profile` переключает; `.harness/hook-mode=learn` понижает структурные до warn (UI-evidence остаётся hard).

Анти-залипание №1 (стоп-сигнал, UserPromptSubmit) и №2 (повтор Bash: инкремент PreToolUse, сброс PostToolUse-успехом) — как handoff: **inject в standard/strict, off в minimal**. Оба warn/inject, никогда не block.

## Что НЕ в таблице (ещё не реализовано — не заявлять как механизм)

Эти из мастер-плана появятся в волнах 1-2 и попадут в таблицу ТОЛЬКО когда будут реальными файлами:
MessageDisplay-детектор языка (H5 — ✅ событие ПОДТВЕРЖДЕНО в 2.1.161; ⚠️ контракт display-only: `displayContent` меняет только экран пользователя, оригинал читает Claude → хуком НЕ enforce'ится поведение модели, только recurrence-метрика + косметика; живой тест payload ПЕРЕД реализацией),
config-protection,
`feature`→Workflow-оркестрация (контракт Workflow-инструмента в скилле — first-use).
До реализации строки тут быть НЕ должно — иначе self-check упадёт на мёртвой ссылке (и это правильно).

> Волна 1 (частично): active-gate H7 + data-model review (строки выше) + агент `data-model-reviewer` —
> сделаны как хук-механизм (не Workflow). `feature` SKILL переписан на правильный порядок (критика → active).
