.DS_Store
*.bak
*.tmp
node_modules/
.env
.env.*
*.pem
*.key
secrets/
# Рантайм-состояние хуков (счётчики/логи/heartbeat — меняются от рутинных операций, НЕ коммитить).
# Конфиг проекта (engine-version, profile, hook-mode) — НЕ здесь, он отслеживается git.
.harness/hooks-heartbeat
.harness/bash-repeat-state
.harness/feature-budget-state
.harness/checkpoint-nudge-at
.harness/stop-chain-count
.harness/stop-cap-log
.harness/clarity-stop-count
.harness/clarity-cap-log
.harness/handoff-pending
.harness/stuck-watcher.pid
.harness/*.log
.harness/hook-crashes/
.harness/cost-log.json
.harness/tools-audit.jsonl
.harness/locks/

# --- Приватная кухня: НЕ уходит в публичный репозиторий (наружу — только чистый плагин) ---
_internal/
_archive/
research/
# Рабочие заметки сессий: несут личные пути и имена клиентских проектов (gate обезличенности
# их не видит — он проверяет только shipped-набор). Наружу не отправляем.
.session-state/T1-*
.session-state/next-*
.session-state/*-progress.md
restart-here.sh
Журналы сессий/
